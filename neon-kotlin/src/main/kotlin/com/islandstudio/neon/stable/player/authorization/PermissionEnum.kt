package com.islandstudio.neon.stable.player.authorization

enum class PermissionEnum(val description: String) {
    ROLE_MANAGEMENT("Ability to manage role operation such as create/remove role as well as assign/unassign role to player.")
    ;
}