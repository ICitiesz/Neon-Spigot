package com.islandstudio.neon.features.neonfeature.gui

import org.bukkit.ChatColor

enum class NeonFeatureSortType(val typeName: String) {
    Default("${ChatColor.WHITE}Default"),
    Stable("${ChatColor.WHITE}Stable"),
    Experimental("${ChatColor.WHITE}Experimental")
}