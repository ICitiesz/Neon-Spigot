package com.islandstudio.neon.stable.primary.nCommand

enum class Commands(val commandAlias: String) {
    WAYPOINTS("waypoints"),
    RANK("rank"),
    DEBUG("debug"),
    EXPERIMENTAL("experimental"),
    GM("gm"),
    REGEN("regen"),
    SERVERCONFIG("serverconfig"),
    EFS("efs"),
    MOD("mod")
}